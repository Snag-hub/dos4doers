# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dos4doers"

# Better Auth
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
BETTER_AUTH_SECRET="..."
BETTER_AUTH_URL="http://localhost:3000"

# Email (Resend)
RESEND_API_KEY="re_..."
EMAIL_FROM="DOs 4 DOERs <noreply@dos4doers.n1k-tech.com>"
EMAIL_REPLY_TO="dos4doers@n1k-tech.com"

# Push Notifications (Web Push - Optional)
NEXT_PUBLIC_VAPID_PUBLIC_KEY="..."
VAPID_PRIVATE_KEY="..."

# Google Calendar Integration (Optional)
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."

# Application URL (used to build links in emails/notifications)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Cron Secret (for API routes)
CRON_SECRET="your-secret-key-here"

# Node Environment
NODE_ENV="development"
